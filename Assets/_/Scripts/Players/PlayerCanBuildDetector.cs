using _.Scripts.GameplayResources;

namespace _.Scripts.Players
{
    public class PlayerCanBuildDetector
    {
        private ResourceContainer _resourceContainer;
        public PlayerCanBuildDetector(ResourceContainer containerForBuild)
        {
            _resourceContainer = containerForBuild;
        }
        
        
    }
}